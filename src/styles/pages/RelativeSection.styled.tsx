import styled from "styled-components";
import StyledSection from "./Section.styled";

const StyledRelativeSection = styled(StyledSection)`
  position: relative;
`;

export default StyledRelativeSection;
